# Local TanStack Start server URL
VITE_SITE_URL=http://localhost:3000

# Convex deployment + URL (generated by `npx convex dev`)
CONVEX_DEPLOYMENT=dev:your-deployment
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Site URL (must end in .convex.site)
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site

# Dev-only: disable auth + use dev user in Convex
# Note: must also set Convex env var: `npx convex env set CLAWDLETS_AUTH_DISABLED true`
# VITE_CLAWDLETS_AUTH_DISABLED=true
# CLAWDLETS_AUTH_DISABLED=true
