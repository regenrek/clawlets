# Terraform + bootstrap inputs
HCLOUD_TOKEN=
ADMIN_CIDR=1.2.3.4/32
# Must be a path to a .pub file (not the key contents)
SSH_PUBKEY_FILE=$HOME/.ssh/id_ed25519.pub
# Default: cx43 (16GB) to avoid OOM during nixos-anywhere remote builds.
# Note: this repo builds x86_64-linux (Intel/AMD). Do not pick CAX (ARM) unless you also switch the flake to aarch64-linux.
SERVER_TYPE=cx43

# If this repo is private on GitHub, Nix remote build needs a token to fetch the flake.
# Use a fine-grained PAT scoped to this repo (Contents: read-only), short expiry, and set:
GITHUB_TOKEN=
